---
layout: base
---

<h1>{{ title }}</h1>
<h2>{{ page.date.toLocaleDateString() }}</h2>

{% if source_url %}
Organiza <a href="{{ source_url }}" title="{{ source_name }}" _blank="true">{{ source_name }}<a/>
{% endif %}

<br />
{{ content | safe }}
